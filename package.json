{
  "name": "resu",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "validate-profile": "npm run validate-profile --workspace=apps/server"
  },
  "packageManager": "npm@11.7.0",
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0",
    "eslint": "^9.20.0",
    "prettier": "^3.5.0"
  }
}
